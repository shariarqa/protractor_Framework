#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../protractor-cucumber-junit/bin/cucumber-junit-enhance" "$@"
  ret=$?
else 
  node  "$basedir/../protractor-cucumber-junit/bin/cucumber-junit-enhance" "$@"
  ret=$?
fi
exit $ret
